<Window x:Class="al_ameer.Features.Login.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Al Ameer - Login" Height="600" Width="1000"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen">

    <Border CornerRadius="30" ClipToBounds="True">
        <Grid>
            <Image Source="https://images.unsplash.com/photo-1549317661-bd32c8ce0db2?q=80&amp;w=2070&amp;auto=format&amp;fit=crop" 
                   Stretch="UniformToFill">
                <Image.Effect>
                    <BlurEffect Radius="20"/>
                </Image.Effect>
            </Image>

            <Border Background="#99000000"/>

            <Grid Margin="50">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="420"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="20">
                    <TextBlock Text="AL AMEER" Foreground="#FFD700" FontSize="54" FontWeight="Black" >
                        <TextBlock.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.5"/>
                        </TextBlock.Effect>
                    </TextBlock>
                    <TextBlock Text="PREMIUM TIRE SERVICES" Foreground="White" FontSize="18" FontWeight="Light" Opacity="0.9" Margin="5,-5,0,30"/>
                    <Rectangle Width="60" Height="4" Fill="#FFD700" HorizontalAlignment="Left" Margin="5,0,0,30"/>
                    <TextBlock Text="Managing your inventory with the&#x0a;luxury and precision it deserves." 
                               Foreground="White" FontSize="16" LineHeight="26" Opacity="0.7" Margin="5,0,0,0"/>
                </StackPanel>

                <Border Grid.Column="1" CornerRadius="25" Padding="45" BorderThickness="1.5">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#F2FFFFFF" Offset="0"/>
                            <GradientStop Color="#D9F7F7F7" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>

                    <Border.BorderBrush>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#80FFFFFF" Offset="0"/>
                            <GradientStop Color="#20FFFFFF" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.BorderBrush>

                    <Border.Effect>
                        <DropShadowEffect BlurRadius="40" ShadowDepth="0" Opacity="0.5" Color="Black"/>
                    </Border.Effect>

                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Staff Login" FontSize="28" FontWeight="Bold" Foreground="#1A1A1A" Margin="0,0,0,8"/>
                        <TextBlock Text="Enter your credentials to continue" Foreground="#777" Margin="0,0,0,35" FontSize="13"/>

                        <TextBlock Text="USERNAME" FontSize="10" FontWeight="Bold" Foreground="#1e3c72" Margin="5,0,0,8" Opacity="0.7"/>
                        <Border Background="#101e3c72" CornerRadius="12" Margin="0,0,0,20" BorderThickness="1" BorderBrush="#15000000">
                            <TextBox x:Name="txtUsername" BorderThickness="0" Background="Transparent" Height="45" 
                                     VerticalContentAlignment="Center" Padding="15,0" FontSize="14" Foreground="#333"/>
                        </Border>

                        <TextBlock Text="PASSWORD" FontSize="10" FontWeight="Bold" Foreground="#1e3c72" Margin="5,0,0,8" Opacity="0.7"/>
                        <Border Background="#101e3c72" CornerRadius="12" Margin="0,0,0,35" BorderThickness="1" BorderBrush="#15000000">
                            <PasswordBox x:Name="txtPassword" BorderThickness="0" Background="Transparent" Height="45" 
                                         VerticalContentAlignment="Center" Padding="15,0" FontSize="14" Foreground="#333"/>
                        </Border>

                        <Button Content="SIGN IN" Height="50" Foreground="White" 
                                FontWeight="Bold" Cursor="Hand" Click="btnLogin_Click" 
                                BorderThickness="0" IsDefault="True">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="#1e3c72"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="border" Background="{TemplateBinding Background}" 
                                                        CornerRadius="12" BorderThickness="0">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="border" Property="Background" Value="#2a5298"/>
                                                        <Setter Property="Effect">
                                                            <Setter.Value>
                                                                <DropShadowEffect BlurRadius="15" Color="#2a5298" Opacity="0.5" ShadowDepth="0"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter TargetName="border" Property="Background" Value="#152e5a"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>

                        <Button Content="Exit Application" Background="Transparent" BorderThickness="0" Foreground="#999" 
                                FontSize="12" Margin="0,25,0,0" Cursor="Hand" Click="Exit_Click"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>