<Window x:Class="al_ameer.Features.Customers.SelectCustomerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Customer" Height="500" Width="400"
        WindowStyle="None" AllowsTransparency="True" Background="#0F0F12"
        WindowStartupLocation="CenterOwner" >
    <Border BorderBrush="#33FFD700" BorderThickness="1" CornerRadius="15">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="SELECT CUSTOMER" Foreground="#FFD700" FontWeight="Bold" Margin="0,0,0,15"/>

            <TextBox x:Name="txtSearch" Grid.Row="1" Height="40" Background="#1A1A1E" 
                     Foreground="White" BorderBrush="#FFD700" Padding="10,0"
                     VerticalContentAlignment="Center" TextChanged="txtSearch_TextChanged"/>

            <ListBox x:Name="lstCustomers" Grid.Row="2" Margin="0,15" Background="Transparent" 
                     BorderThickness="0" Foreground="White" DisplayMemberPath="FullName"
                     MouseDoubleClick="lstCustomers_MouseDoubleClick">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="10"/>
                        <Setter Property="Margin" Value="0,2"/>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Click="Cancel_Click" Background="Transparent" Foreground="White" Margin="0,0,10,0" BorderThickness="0"/>
                <Button Content="Select" Click="Select_Click" Width="100" Height="35" Background="#FFD700" Foreground="Black" FontWeight="Bold"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>